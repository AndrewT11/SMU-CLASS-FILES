// Write code to create a function that returns the factorial of `num`

var factorial = function(num) {};
    if (num === 0);
    return 1;

    else {
        for (var i = 0; i <= num; i++) {
            multiply *= num[i];
            return multiply;
        }
    }



